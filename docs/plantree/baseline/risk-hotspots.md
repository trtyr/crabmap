# Risk Hotspots

## 健康评分：97/100 (high)

## 循环依赖（0 个）

所有循环依赖已通过健康检查器准确性优化消除。原 16 个循环均为假阳性：
- trait impl 方法误解析（`.from()` 等通用方法名）
- 模块/文件声明节点创建的伪依赖
- 推断/可能边不应参与循环检测

## God Modules（1 个）

### 低风险

1. **analyzer**（1266 行，42 有意义符号，90 总符号）
   - 状态：已拆分为 6 个子模块（index, builder, helpers, visitors, types, resolution）
   - 问题：组合后仍较大，但组织良好
   - 建议：无需进一步拆分

## 热点文件（高风险）

| 文件 | Degree | 风险评分 | 说明 |
|------|--------|----------|------|
| `model.rs` | 535 | 536 | 数据模型，所有模块依赖 |
| `cli.rs` | 464 | 465 | 命令定义，影响用户接口 |
| `analyzer/` | 559 | 560 | 核心索引器，修改影响全局 |
| `web/` | 319 | 320 | Web 服务器，用户界面 |
| `query/` | 233 | 234 | 查询逻辑，核心功能 |
| `semantic/` | 215 | 216 | 语义分析，增强功能 |
| `tests/cli.rs` | 187 | 189 | 集成测试 |
| `main.rs` | 178 | 179 | 入口点 |
| `rag/` | 174 | 175 | RAG 功能 |
| `ai/` | 171 | 172 | AI 导航 |

## 可能的死代码（0 个）

所有死代码告警已通过以下改进消除：
1. 过滤 `pub(crate)` 可见性符号
2. 排除测试 fixtures
3. 检查符号是否有任意图边（而非仅入边）
4. 排除有边的符号（已连接到图）

## 健康检查器改进

### 死代码检测
- 过滤 `pub(crate)`、`pub(super)` 等受限可见性
- 排除 `tests/fixtures/` 目录下的符号
- 检查符号是否有任意边（入边或出边），而非仅入边

### 循环检测
- 排除 trait impl 方法（如 `From::from`、`Into::into`）的误解析边
- 排除模块/文件/项目/crate 声明节点
- 排除推断（Inferred）和可能（Possible）确定性的边
- 按模块目录分组，而非单个文件

### God Module 检测
- 只计有意义符号（函数、方法、trait、impl、macro）
- 不计字段、变体等类型细节
- 行数阈值从 800 提升到 1200
- 有意义符号阈值 >=40

## 最后更新

2026-05-28
